import os

CSV_directory = os.path.join(os.getcwd(), 'Toolbox', 'CSV')
os.makedirs(CSV_directory, exist_ok=True)

# opening the csv file

# read file structure and composition of each line

# check if the fields username password, site url, or other entries exist
